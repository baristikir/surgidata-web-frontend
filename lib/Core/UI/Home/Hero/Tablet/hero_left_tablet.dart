import 'package:flutter/material.dart';

class HomeHeroLeftTablet extends StatelessWidget {
  const HomeHeroLeftTablet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(),
    );
  }
}